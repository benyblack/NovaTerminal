name: CI

on:
  pull_request:
    paths-ignore:
      - "documents/**"
      - "**/*.md"
  push:
    branches: [main]
    paths-ignore:
      - "documents/**"
      - "**/*.md"
  schedule:
    - cron: "30 3 * * *"
  workflow_dispatch:

# No job here writes to the repository: every step is build/test plus
# checkout/setup-dotnet/cache/upload-artifact, none of which touch GITHUB_TOKEN
# beyond reading the source. Grant the minimum and let any future job that needs
# more opt in with its own job-level `permissions:` block.
permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

env:
  DOTNET_NOLOGO: true
  DOTNET_CLI_TELEMETRY_OPTOUT: true
  CONFIGURATION: Release
  # Opt into Node.js 24 now; GitHub Actions will force this on 2026-06-02.
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
  # Prevent dotnet/MSBuild daemons from outliving each step and holding the
  # GitHub Actions runner's redirected stdout/stderr pipes open. Without these,
  # Linux Unit Tests has historically hung after tests completed because the
  # MSBuild worker pool spawned by `dotnet test` (and any nested `dotnet build`
  # inside tests like VtReportCliTests) kept the pipe alive past the step
  # timeout. UseSharedCompilation is also disabled globally via
  # Directory.Build.props, but these env vars cover the driver-level daemons
  # (MSBuild build server, MSBuild worker node reuse) that project files
  # cannot influence.
  DOTNET_CLI_USE_MSBUILD_SERVER: '0'
  MSBUILDDISABLENODEREUSE: '1'

jobs:
  # Native FFI unit/integration tests (handle registry, abuse suite, alloc balance).
  # The per-OS build job only `cargo build`s the crates; the cargo test suites
  # (rusty_ssh fail-closed handle tests + rusty_pty cancel tests) run here.
  #
  # Runs on Windows as well as Linux: rusty_pty's ConPTY code is behind #[cfg(windows)], so its
  # tests - including the Win32 handle-leak regression test added for #120 - are compiled out
  # entirely on Linux and were never actually exercised by CI.
  rust-ffi-tests:
    name: Rust FFI Tests (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
    steps:
      - uses: actions/checkout@v4
      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
      - name: Rust incremental build cache
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: |
            src/NovaTerminal.App/native
            src/NovaTerminal.App/native/rusty_ssh
      - name: rusty_ssh FFI tests (registry, abuse suite, alloc balance)
        run: cargo test --manifest-path src/NovaTerminal.App/native/rusty_ssh/Cargo.toml
      - name: rusty_pty FFI tests
        run: cargo test --manifest-path src/NovaTerminal.App/native/Cargo.toml

  # Single build job per OS: Rust native + .NET in one step.
  # Eliminates the artifact round-trip that the old build_native → build_dotnet split required.
  build:
    name: Build (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        os: ${{ fromJSON(github.event_name == 'workflow_dispatch' && '["windows-latest","ubuntu-latest","macos-latest"]' || '["windows-latest","ubuntu-latest"]') }}

    steps:
      - uses: actions/checkout@v4

      # ── Rust native ────────────────────────────────────────────────────────────
      # Cache the compiled binary keyed on all Rust source files.
      # On a hit the toolchain install, Swatinem, and cargo build are all skipped.
      # Cache both target/release and target_linux/release: the Linux csproj
      # consumes the .so from target_linux/release, so it must be part of the
      # cache or a cache hit leaves the staged path empty and the Build step
      # below is skipped. The v2 bump invalidated older caches that contained only
      # target/release; the v3 bump on the key itself is explained there.
      - name: Cache native binary
        id: native-bin-cache
        uses: actions/cache@v4
        with:
          path: |
            src/NovaTerminal.App/native/target/release
            src/NovaTerminal.App/native/target_linux/release
          # Key version bumped to v3 for the rusty_pty input-hash stamp
          # (native/target/release/.nova-rust-inputs.sha256). A v2 payload predates the stamp,
          # so restoring one would make BuildRustNative call the binary stale and invoke cargo -
          # on a runner where this same cache hit skipped the toolchain install and the cargo
          # registry cache. Worse, actions/cache does not re-save on an exact key hit, so the
          # stamp would never enter the v2 payload and every hit would pay that rebuild forever.
          # Bumping forces one miss, whose save captures the stamp.
          key: native-bin-v3-${{ matrix.os }}-${{ hashFiles('src/NovaTerminal.App/native/src/**', 'src/NovaTerminal.App/native/Cargo.toml', 'src/NovaTerminal.App/native/Cargo.lock') }}

      - name: Install Rust
        if: steps.native-bin-cache.outputs.cache-hit != 'true'
        uses: dtolnay/rust-toolchain@stable

      - name: Rust incremental build cache
        if: steps.native-bin-cache.outputs.cache-hit != 'true'
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: src/NovaTerminal.App/native

      - name: Build Rust (Windows)
        if: matrix.os == 'windows-latest' && steps.native-bin-cache.outputs.cache-hit != 'true'
        run: cd src/NovaTerminal.App/native && cargo build --release

      # Linux build also stages the .so into target_linux/release so the
      # cached payload is complete and no separate stage step is needed.
      - name: Build Rust (Linux)
        if: matrix.os == 'ubuntu-latest' && steps.native-bin-cache.outputs.cache-hit != 'true'
        run: |
          cd src/NovaTerminal.App/native
          cargo build --release
          mkdir -p target_linux/release
          cp target/release/librusty_pty.so target_linux/release/

      - name: Build Rust (macOS)
        if: matrix.os == 'macos-latest' && steps.native-bin-cache.outputs.cache-hit != 'true'
        run: cd src/NovaTerminal.App/native && cargo build --release

      # ── .NET ───────────────────────────────────────────────────────────────────
      - name: Cache .NET SDK
        uses: actions/cache@v4
        with:
          path: ~/.dotnet
          key: dotnet-sdk-${{ runner.os }}-${{ hashFiles('global.json') }}
          restore-keys: dotnet-sdk-${{ runner.os }}-

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        env:
          DOTNET_INSTALL_DIR: ~/.dotnet
        with:
          global-json-file: global.json
          cache: true
          cache-dependency-path: "**/*.csproj"

      - name: Restore
        run: dotnet restore

      - name: Build
        run: dotnet build -c ${{ env.CONFIGURATION }} --no-restore

      # Upload only the test output directories. src/*/bin/ is 100% duplicated inside
      # tests/NovaTerminal.App.Tests/bin/ (MSBuild copies all referenced DLLs + executables),
      # so including it would double the artifact size for no benefit.
      # VtReportCliTests needs src/NovaTerminal.App/bin/ at runtime — the unit test
      # job restores it from the test output via a symlink/junction (see below).
      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: dotnet-build-${{ matrix.os }}
          retention-days: 1
          # upload-artifact@v4 strips the longest common ancestor of all paths, so
          # "tests/" is stripped from both entries here. All download-artifact steps
          # compensate by specifying path: tests to restore the prefix.
          path: |
            tests/NovaTerminal.App.Tests/bin/${{ env.CONFIGURATION }}
            tests/NovaTerminal.Platform.Tests/bin/${{ env.CONFIGURATION }}
            tests/NovaTerminal.Architecture.Tests/bin/${{ env.CONFIGURATION }}
            tests/NovaTerminal.VT.Tests/bin/${{ env.CONFIGURATION }}
            tests/NovaTerminal.Rendering.Tests/bin/${{ env.CONFIGURATION }}
            tests/NovaTerminal.McpServer.Tests/bin/${{ env.CONFIGURATION }}

  build_and_unit_tests:
    name: Unit Tests (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    timeout-minutes: 15
    needs: [build]
    strategy:
      fail-fast: false
      matrix:
        # Same dispatch-conditional shape as `build` above, and for the same reason: macOS is
        # not worth the minutes on every PR, but it must be reachable WITHOUT cutting a
        # release. Until now `build` compiled on macOS while nothing ever ran the tests there,
        # so macOS's only test coverage was release.yml's release_tests — meaning a
        # macOS-only defect could surface exactly once, at release time, where it blocks the
        # release. That is how #325's port-forward backpressure test went three months
        # unverified on macOS and then failed the first release that ran it.
        os: ${{ fromJSON(github.event_name == 'workflow_dispatch' && '["windows-latest","ubuntu-latest","macos-latest"]' || '["windows-latest","ubuntu-latest"]') }}

    steps:
      - uses: actions/checkout@v4

      - name: Cache .NET SDK
        uses: actions/cache@v4
        with:
          path: ~/.dotnet
          key: dotnet-sdk-${{ runner.os }}-${{ hashFiles('global.json') }}
          restore-keys: dotnet-sdk-${{ runner.os }}-

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        env:
          DOTNET_INSTALL_DIR: ~/.dotnet
        with:
          global-json-file: global.json
          cache: true
          cache-dependency-path: "**/*.csproj"

      - name: Download build artifact
        uses: actions/download-artifact@v4
        with:
          name: dotnet-build-${{ matrix.os }}
          # upload-artifact@v4 strips the longest common ancestor of all uploaded paths.
          # Both uploaded paths share "tests/" so it gets stripped inside the zip.
          # Downloading to "tests" restores the expected tests/* structure.
          path: tests

      # zip artifacts don't preserve Unix execute bits; restore them so the test host and
      # any app/CLI binaries referenced by tests (NovaTerminal.App, NovaTerminal.Cli) can run.
      #
      # Every POSIX runner needs this, not just Linux. While this job was windows+ubuntu only,
      # "Linux" and "not Windows" were the same set; adding macOS separated them, and macOS
      # skipping it failed the whole job with "Win32Exception (13) ... Permission denied" trying
      # to launch the xunit v3 test host. `-exec … +` rather than `| xargs`, because xargs with
      # no matches still invokes chmod with no operands, which fails on BSD/macOS.
      - name: Restore execute permissions (POSIX)
        if: runner.os != 'Windows'
        run: |
          find tests -path "*/bin/Release/net*" -type f \
            ! -name "*.dll" ! -name "*.json" ! -name "*.pdb" ! -name "*.xml" \
            ! -name "*.so" ! -name "*.dylib" ! -name "*.a" ! -name "*.txt" \
            -exec chmod +x {} +

      # VtReportCliTests.cs looks for app/CLI binaries in src/*/bin/Release/net10.0/.
      # Every file there is already present in the test output (MSBuild copies all
      # referenced DLLs and the host executable), so we mirror it locally with
      # hardlinks (Linux) or robocopy (Windows) instead of duplicating 1.1 GB in
      # the artifact. Local disk copy is fast; network upload/download is not.
      - name: Reconstruct src bin dirs from test output (POSIX)
        if: runner.os != 'Windows'
        run: |
          mkdir -p src/NovaTerminal.App/bin/Release src/NovaTerminal.Cli/bin/Release
          cp -rl tests/NovaTerminal.App.Tests/bin/Release/net10.0 src/NovaTerminal.App/bin/Release/net10.0
          cp -rl tests/NovaTerminal.App.Tests/bin/Release/net10.0 src/NovaTerminal.Cli/bin/Release/net10.0

      - name: Reconstruct src bin dirs from test output (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $src = "tests\NovaTerminal.App.Tests\bin\Release\net10.0"
          # robocopy exits 0 (nothing to copy) or 1 (files copied) on success; >=8 is an error
          foreach ($dst in @("src\NovaTerminal.App\bin\Release\net10.0", "src\NovaTerminal.Cli\bin\Release\net10.0")) {
            robocopy $src $dst /E /NJH /NJS /NFL /NDL /NC /NS | Out-Null
            if ($LASTEXITCODE -ge 8) { exit $LASTEXITCODE }
          }
          exit 0

      - name: Restore
        run: dotnet restore

      # The shell-integration tests under tests/NovaTerminal.App.Tests/CommandAssist/ShellIntegration/Integration
      # spawn real bash/zsh/fish via the production PTY path. bash is
      # preinstalled on ubuntu-latest; zsh and fish are not on ubuntu-24.04,
      # so install them here -- otherwise ZshShellIntegrationTests and
      # FishShellIntegrationTests skip instead of running.
      - name: Install zsh and fish (Linux)
        if: runner.os == 'Linux'
        run: sudo apt-get update && sudo apt-get install -y zsh fish

      # Gating lane: the non-headless test projects. These are deterministic and
      # MUST pass. The filter drops the lanes that run in their own jobs (replay,
      # render-metrics, pty-smoke, stress, golden-shared-png).
      - name: Test (unit — gating, non-headless projects)
        env:
          SKIP_RUST_NATIVE_BUILD: '1'
        shell: bash
        run: |
          set -e
          filter="Category!=Replay&Category!=RenderMetrics&Category!=PtySmoke&Category!=Stress&Category!=GoldenSharedPng"
          for proj in VT Rendering Architecture Platform McpServer; do
            echo "::group::NovaTerminal.$proj.Tests"
            dotnet test "tests/NovaTerminal.$proj.Tests" -c "$CONFIGURATION" --no-build --no-restore \
              --filter "$filter" --logger "trx;LogFileName=unit_${proj}.trx"
            echo "::endgroup::"
          done

      # NON-BLOCKING lane: the Avalonia-headless suite (NovaTerminal.App.Tests).
      # It intermittently deadlocks on a testhost teardown hang — a dump-confirmed
      # framework deadlock in Avalonia.Headless.XUnit 12.0.4 (the latest release),
      # reported upstream as AvaloniaUI/Avalonia#21467 and tracked here as #81. No
      # in-repo config fixes it (collection serialization, maxParallelThreads:1, and
      # ThreadPool.SetMinThreads were all ruled out with dump evidence). Until an
      # upstream fix lands, run it as continue-on-error so the flake can't block PRs,
      # while still executing it: a clean run reports real pass/fail, a genuine
      # regression shows as a failed (non-blocking) check, and each hang writes a
      # mini dump (uploaded below) for ongoing diagnosis.
      - name: Test (headless App.Tests — NON-BLOCKING, see #81 / AvaloniaUI/Avalonia#21467)
        continue-on-error: true
        env:
          SKIP_RUST_NATIVE_BUILD: '1'
        shell: bash
        run: |
          dotnet test tests/NovaTerminal.App.Tests -c "$CONFIGURATION" --no-build --no-restore \
            --filter "Category!=Replay&Category!=RenderMetrics&Category!=PtySmoke&Category!=Stress&Category!=GoldenSharedPng" \
            --blame-hang-timeout 5m --blame-hang-dump-type mini \
            --logger "trx;LogFileName=unit_app.trx"

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: unit-tests-${{ runner.os }}
          retention-days: 5
          # *.dmp captures the blame-hang minidump on a teardown hang (#81).
          path: |
            **/*.trx
            **/*.dmp

  # Native SSH end-to-end against a real sshd in a container.
  #
  # These tests existed before this job did, and never ran anywhere: every one is a
  # [DockerFact], which self-skips unless NOVATERM_ENABLE_DOCKER_E2E=1, and nothing in CI set
  # it. The suite was written, committed, skipped, and counted as coverage — which is why the
  # test matrix still listed auth, host-key and forwarding as "pending manual" while a
  # Dockerized suite sat next to it. This job is what makes the difference between the two
  # visible.
  #
  # Deliberately blocking rather than continue-on-error. A non-blocking lane here would repeat
  # the mistake #127 caught in the Render Metrics job: a check whose name claims coverage it
  # does not enforce. If it turns flaky, the fix is to stabilise the fixture, not to silence
  # the gate.
  #
  # Linux only: the tests drive `docker` through the CLI, and ubuntu-latest is the runner image
  # with a working Linux Docker daemon. The Windows runners would need Linux-container support
  # that is not available there, and nothing under test is platform-specific — the fixture
  # exercises the same rusty_ssh library either way.
  native_ssh_docker_e2e:
    name: Native SSH Docker E2E (ubuntu-latest)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    needs: [build]

    steps:
      - uses: actions/checkout@v4

      - name: Cache .NET SDK
        uses: actions/cache@v4
        with:
          path: ~/.dotnet
          key: dotnet-sdk-${{ runner.os }}-${{ hashFiles('global.json') }}
          restore-keys: dotnet-sdk-${{ runner.os }}-

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        env:
          DOTNET_INSTALL_DIR: ~/.dotnet
        with:
          global-json-file: global.json
          cache: true
          cache-dependency-path: "**/*.csproj"

      - name: Download build artifact
        uses: actions/download-artifact@v4
        with:
          name: dotnet-build-ubuntu-latest
          path: tests

      # zip artifacts drop Unix execute bits; the test host needs them back. librusty_ssh.so
      # travels in this artifact too (Platform.Tests copies it as Content), so the P/Invoke
      # surface these tests exercise is already in place — no Rust rebuild needed.
      - name: Restore execute permissions
        run: |
          find tests -path "*/bin/Release/net*" -type f \
            ! -name "*.dll" ! -name "*.json" ! -name "*.pdb" ! -name "*.xml" \
            ! -name "*.so" ! -name "*.dylib" ! -name "*.a" ! -name "*.txt" \
            | xargs chmod +x

      - name: Docker preflight
        run: docker info --format 'server={{.ServerVersion}}'

      - name: Restore
        run: dotnet restore

      # The fixture builds the image itself on first use (DockerSshFixture.EnsureImageBuiltAsync),
      # so there is no separate build step here; a failure to build surfaces as a test failure
      # with the docker output attached.
      - name: Test (native SSH Docker E2E)
        env:
          NOVATERM_ENABLE_DOCKER_E2E: '1'
          SKIP_RUST_NATIVE_BUILD: '1'
        shell: bash
        run: |
          dotnet test tests/NovaTerminal.Platform.Tests -c "$CONFIGURATION" --no-build --no-restore \
            --filter "Category=DockerE2E" \
            --logger "trx;LogFileName=native_ssh_docker_e2e.trx"

      # No "collect container logs" step on purpose: the fixture runs containers with --rm and
      # removes them in DisposeAsync, which still executes when a test fails, so by the time a
      # post-step ran there would be nothing left to read. The diagnostics live where they are
      # actually available instead — DockerSshFixture attaches `docker logs` output to the
      # readiness timeouts it throws, so the failure reaches the .trx with the logs in it.
      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: native-ssh-docker-e2e
          retention-days: 5
          path: "**/native_ssh_docker_e2e.trx"

  # Coverage measurement (#117). coverlet.collector was pinned in
  # Directory.Packages.props and referenced by all five test projects, but nothing ever
  # enabled it - no invocation passed a collect switch and no report was produced.
  #
  # Deliberately a separate job rather than instrumenting the gating Unit Tests lane.
  # Coverage instrumentation slows execution by orders of magnitude on hot loops, and the
  # suite contains wall-clock assertions - CsiParamClampTests.HugeCsiParameter_DoesNotHang
  # guards the #124 parser DoS with a 2s ceiling, and measured 2088ms under instrumentation
  # locally. Instrumenting the gate would make the gate's own timing assertions unreliable,
  # so correctness and measurement are kept apart.
  # Formatting was previously checked only by ci/run.{ps1,sh}, and those were deliberately
  # report-only because main carried 674 pre-existing whitespace violations. Nothing in
  # GitHub CI looked at formatting at all, so the count drifted upward unnoticed (649 -> 674
  # over two weeks with nobody working on it). #216 swept the backlog; this job is the half
  # that stops it re-accumulating.
  #
  # Runs on both OSes on purpose. `.editorconfig` requires CRLF while git stores LF, so
  # before `.gitattributes` pinned the checkout this check reached a different verdict per
  # platform. The ubuntu leg is what proves that pinning works.
  #
  # Scoped to `whitespace`: a bare `dotnet format` also runs the style and analyzer passes,
  # whose diagnostics belong to #108. Mixing them would make this gate fail for reasons
  # unrelated to formatting.
  format:
    name: Format (whitespace)
    runs-on: ${{ matrix.os }}
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, ubuntu-latest]

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          global-json-file: global.json
          cache: true
          cache-dependency-path: "**/*.csproj"

      - name: Restore
        run: dotnet restore

      - name: Verify formatting
        run: dotnet format whitespace --verify-no-changes --no-restore

  coverage:
    name: Coverage (NovaTerminal.VT floor)
    runs-on: ubuntu-latest
    timeout-minutes: 20
    needs: [build]

    steps:
      - uses: actions/checkout@v4

      - name: Cache .NET SDK
        uses: actions/cache@v4
        with:
          path: ~/.dotnet
          key: dotnet-sdk-${{ runner.os }}-${{ hashFiles('global.json') }}
          restore-keys: dotnet-sdk-${{ runner.os }}-

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        env:
          DOTNET_INSTALL_DIR: ~/.dotnet
        with:
          global-json-file: global.json
          cache: true
          cache-dependency-path: "**/*.csproj"

      # Builds from source rather than consuming the build job's artifact. Two reasons:
      #
      # 1. `dotnet test <dir> --no-build --settings <file>` silently does nothing on this
      #    SDK - it exits 0 in ~0.3s with no output and writes no report, five times over.
      #    The gating lane gets away with the same --no-build because it passes no
      #    --settings. Building here matches the invocation shape that was verified to
      #    produce a report locally, instead of relying on that combination working.
      # 2. Coverage needs the source tree anyway to attribute lines, so the artifact saved
      #    nothing here.
      #
      # The cost is one extra build in exchange for a job that demonstrably measures.
      - name: Restore
        run: dotnet restore

      - name: Build
        run: dotnet build -c ${{ env.CONFIGURATION }} --no-restore

      # continue-on-error: this job measures, it does not judge correctness - the Unit Tests
      # lane already does that on an uninstrumented run. Without this, a wall-clock
      # assertion tripped by instrumentation overhead (see the job comment) would fail here
      # for a reason that says nothing about the code. Coverage data is still written for
      # every test that ran, so the floor check below remains meaningful.
      - name: Test with coverage
        continue-on-error: true
        env:
          SKIP_RUST_NATIVE_BUILD: '1'
        shell: bash
        run: |
          # No `|| true` inside the loop. The step is already continue-on-error, so
          # swallowing exit codes there only destroys the diagnostics - which is exactly
          # what happened on the first attempt: five empty log groups and no way to tell
          # why. set -x records the resolved command so a failure explains itself.
          set -x
          dotnet --version
          filter="Category!=Replay&Category!=RenderMetrics&Category!=PtySmoke&Category!=Stress&Category!=GoldenSharedPng"
          # Which projects are measured, and why these:
          #
          # Architecture.Tests is deliberately absent. It has no coverlet.collector
          # reference (unlike the four below), so it produced no report and the job
          # measured four projects while listing five. It asserts project and IL structure
          # rather than executing product code, so there is little for coverlet to
          # attribute - excluding it is more honest than adding the package to make an
          # empty report appear.
          #
          # App.Tests is also absent, matching the gating lane, which excludes it because
          # of the Avalonia headless flake (#81). It does carry the collector, and it
          # exercises a lot of VT (the buffer and reflow scenario suites live there) - so
          # its absence is the main reason the VT figure below understates true coverage.
          for proj in VT Rendering Platform McpServer; do
            echo "::group::NovaTerminal.$proj.Tests"
            dotnet test "tests/NovaTerminal.$proj.Tests" -c "$CONFIGURATION" --no-restore \
              --filter "$filter" \
              --settings tests/coverage.runsettings \
              --results-directory "coverage/$proj"
            echo "::endgroup::"
          done
          echo "=== collected reports ==="
          find coverage -name '*.cobertura.xml' -print || echo "none found"

      # A floor, not a target, and deliberately below the measured value: VT is at 53.14%
      # line / 46.51% branch as measured by this invocation, so the floor is 50% - normal
      # churn cannot turn a green PR red, and it should be ratcheted up as coverage grows
      # rather than set aspirationally now. VT gets the only floor because it is the
      # conformance-critical assembly; the other four upload their reports so their numbers
      # are visible before anyone commits to a floor for them.
      - name: Check coverage floor (NovaTerminal.VT)
        shell: pwsh
        run: >-
          ./scripts/check-coverage.ps1
          -ReportDirectory coverage/VT
          -MinimumLinePercent 50
          -Label NovaTerminal.VT

      - name: Upload coverage reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: coverage-reports
          retention-days: 5
          path: coverage/**/*.cobertura.xml

  golden_shared_png_tests:
    name: Golden Shared PNGs (windows-latest)
    runs-on: windows-latest
    timeout-minutes: 10
    needs: [build]

    steps:
      - uses: actions/checkout@v4

      - name: Cache .NET SDK
        uses: actions/cache@v4
        continue-on-error: true
        with:
          path: ~/.dotnet
          key: dotnet-sdk-${{ runner.os }}-${{ hashFiles('global.json') }}
          restore-keys: dotnet-sdk-${{ runner.os }}-

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        env:
          DOTNET_INSTALL_DIR: ~/.dotnet
        with:
          global-json-file: global.json
          cache: true
          cache-dependency-path: "**/*.csproj"

      - name: Download build artifact
        id: download_golden_build_artifact
        uses: actions/download-artifact@v4
        continue-on-error: true
        with:
          name: dotnet-build-windows-latest
          path: tests

      - name: Reset partial artifact download
        if: steps.download_golden_build_artifact.outcome == 'failure'
        shell: pwsh
        run: |
          Remove-Item tests\NovaTerminal.App.Tests\bin -Recurse -Force -ErrorAction SilentlyContinue
          Remove-Item tests\NovaTerminal.Platform.Tests\bin -Recurse -Force -ErrorAction SilentlyContinue

      - name: Download build artifact (retry)
        if: steps.download_golden_build_artifact.outcome == 'failure'
        uses: actions/download-artifact@v4
        with:
          name: dotnet-build-windows-latest
          path: tests

      - name: Restore
        run: dotnet restore

      - name: Run Golden Shared PNG Tests
        run: dotnet test -c ${{ env.CONFIGURATION }} --no-build --no-restore --filter "Category=GoldenSharedPng" --blame-hang-timeout 8m --blame-hang-dump-type none --logger "trx;LogFileName=golden_shared_png_tests.trx"

      - name: Upload golden shared PNG test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: golden-shared-png-tests-windows
          retention-days: 5
          path: "**/*golden_shared_png_tests.trx"

  replay_tests:
    name: Replay Tests (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    if: github.event_name != 'pull_request'
    timeout-minutes: 15
    needs: [build]
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, ubuntu-latest]

    steps:
      - uses: actions/checkout@v4

      - name: Cache .NET SDK
        uses: actions/cache@v4
        with:
          path: ~/.dotnet
          key: dotnet-sdk-${{ runner.os }}-${{ hashFiles('global.json') }}
          restore-keys: dotnet-sdk-${{ runner.os }}-

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        env:
          DOTNET_INSTALL_DIR: ~/.dotnet
        with:
          global-json-file: global.json
          cache: true
          cache-dependency-path: "**/*.csproj"

      - name: Download build artifact
        uses: actions/download-artifact@v4
        with:
          name: dotnet-build-${{ matrix.os }}
          path: tests

      - name: Restore execute permissions (Linux)
        if: runner.os == 'Linux'
        run: |
          find tests -path "*/bin/Release/net*" -type f \
            ! -name "*.dll" ! -name "*.json" ! -name "*.pdb" ! -name "*.xml" \
            ! -name "*.so" ! -name "*.dylib" ! -name "*.a" ! -name "*.txt" \
            | xargs chmod +x

      - name: Restore
        run: dotnet restore

      - name: Run Replay Tests
        env:
          PARITY_ARTIFACT_DIR: ${{ github.workspace }}/artifacts/parity
        run: dotnet test -c ${{ env.CONFIGURATION }} --no-build --no-restore --filter "Category=Replay" --blame-hang-timeout 8m --blame-hang-dump-type none --logger "trx;LogFileName=replay_tests.trx"

      - name: Prepare Replay Artifact Bundle
        if: always()
        shell: pwsh
        run: |
          $bundleDir = "artifacts/parity"
          New-Item -ItemType Directory -Force -Path $bundleDir | Out-Null
          "runner_os=${{ runner.os }}; matrix_os=${{ matrix.os }}; run_id=${{ github.run_id }}" |
            Set-Content -Path (Join-Path $bundleDir "_artifact_meta.txt")

      - name: Upload Replay Artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: replay-snapshots-${{ matrix.os }}
          retention-days: 5
          path: |
            artifacts/parity/*.snap
            artifacts/parity/**/*.snap
            artifacts/parity/_artifact_meta.txt
            **/*replay_tests.trx

  pty_smoke_tests:
    name: PTY Smoke Tests (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    if: github.event_name == 'pull_request' || github.event_name == 'push' || github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
    timeout-minutes: 10
    needs: [build]
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, ubuntu-latest]

    steps:
      - uses: actions/checkout@v4

      - name: Cache .NET SDK
        uses: actions/cache@v4
        with:
          path: ~/.dotnet
          key: dotnet-sdk-${{ runner.os }}-${{ hashFiles('global.json') }}
          restore-keys: dotnet-sdk-${{ runner.os }}-

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        env:
          DOTNET_INSTALL_DIR: ~/.dotnet
        with:
          global-json-file: global.json
          cache: true
          cache-dependency-path: "**/*.csproj"

      - name: Download build artifact
        uses: actions/download-artifact@v4
        with:
          name: dotnet-build-${{ matrix.os }}
          path: tests

      - name: Restore execute permissions (Linux)
        if: runner.os == 'Linux'
        run: |
          find tests -path "*/bin/Release/net*" -type f \
            ! -name "*.dll" ! -name "*.json" ! -name "*.pdb" ! -name "*.xml" \
            ! -name "*.so" ! -name "*.dylib" ! -name "*.a" ! -name "*.txt" \
            | xargs chmod +x

      - name: Restore
        run: dotnet restore

      - name: Run PTY smoke tests
        run: dotnet test -c ${{ env.CONFIGURATION }} --no-build --no-restore --filter "Category=PtySmoke" --blame-hang-timeout 8m --blame-hang-dump-type none --logger "trx;LogFileName=pty_smoke_tests.trx"

      - name: Upload PTY smoke test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pty-smoke-${{ runner.os }}
          retention-days: 5
          path: "**/*pty_smoke_tests.trx"

  parity_compare:
    name: Cross-Platform Parity Compare
    runs-on: ubuntu-latest
    if: github.event_name != 'pull_request'
    timeout-minutes: 15
    needs: [replay_tests]

    steps:
      - uses: actions/checkout@v4

      - name: Download snapshots (Windows)
        uses: actions/download-artifact@v4
        with:
          name: replay-snapshots-windows-latest
          path: parity/windows

      - name: Download snapshots (Linux)
        uses: actions/download-artifact@v4
        with:
          name: replay-snapshots-ubuntu-latest
          path: parity/linux

      - name: Inspect downloaded parity artifacts
        run: |
          echo "Downloaded files:"
          find parity -type f | sort || true
          echo "Snapshot counts:"
          echo "windows=$(find parity/windows -type f -name '*.snap' | wc -l)"
          echo "linux=$(find parity/linux -type f -name '*.snap' | wc -l)"

      - name: Compare snapshots for parity
        run: |
          python3 tests/tools/compare_snapshots.py parity/windows parity/linux

  render_metrics_tests:
    name: Render Metrics (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    if: github.event_name == 'pull_request' || github.event_name == 'push' || github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
    timeout-minutes: 10
    needs: [build]
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, ubuntu-latest]

    steps:
      - uses: actions/checkout@v4

      - name: Cache .NET SDK
        uses: actions/cache@v4
        with:
          path: ~/.dotnet
          key: dotnet-sdk-${{ runner.os }}-${{ hashFiles('global.json') }}
          restore-keys: dotnet-sdk-${{ runner.os }}-

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        env:
          DOTNET_INSTALL_DIR: ~/.dotnet
        with:
          global-json-file: global.json
          cache: true
          cache-dependency-path: "**/*.csproj"

      - name: Download build artifact
        uses: actions/download-artifact@v4
        with:
          name: dotnet-build-${{ matrix.os }}
          path: tests

      - name: Restore execute permissions (Linux)
        if: runner.os == 'Linux'
        run: |
          find tests -path "*/bin/Release/net*" -type f \
            ! -name "*.dll" ! -name "*.json" ! -name "*.pdb" ! -name "*.xml" \
            ! -name "*.so" ! -name "*.dylib" ! -name "*.a" ! -name "*.txt" \
            | xargs chmod +x

      - name: Restore
        run: dotnet restore

      - name: Run Render Metrics Tests
        env:
          WRITE_METRICS_ARTIFACTS: "1"
          METRICS_ARTIFACT_DIR: ${{ github.workspace }}/artifacts/metrics
        run: dotnet test -c ${{ env.CONFIGURATION }} --no-build --no-restore --filter "Category=RenderMetrics" --blame-hang-timeout 8m --blame-hang-dump-type none --logger "trx;LogFileName=render_metrics.trx"

      - name: Upload render metrics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: render-metrics-${{ runner.os }}
          retention-days: 5
          path: |
            artifacts/metrics/**
            **/*render_metrics.trx

  tab_perf_smoke:
    name: Tab Perf Smoke
    runs-on: ubuntu-latest
    timeout-minutes: 10
    if: github.event_name == 'pull_request' || github.event_name == 'push'
    needs: [build]

    steps:
      - uses: actions/checkout@v4

      - name: Cache .NET SDK
        uses: actions/cache@v4
        with:
          path: ~/.dotnet
          key: dotnet-sdk-${{ runner.os }}-${{ hashFiles('global.json') }}
          restore-keys: dotnet-sdk-${{ runner.os }}-

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        env:
          DOTNET_INSTALL_DIR: ~/.dotnet
        with:
          global-json-file: global.json
          cache: true
          cache-dependency-path: "**/*.csproj"

      - name: Download build artifact
        uses: actions/download-artifact@v4
        with:
          name: dotnet-build-ubuntu-latest
          path: tests

      - name: Restore execute permissions
        run: |
          find tests -path "*/bin/Release/net*" -type f \
            ! -name "*.dll" ! -name "*.json" ! -name "*.pdb" ! -name "*.xml" \
            ! -name "*.so" ! -name "*.dylib" ! -name "*.a" ! -name "*.txt" \
            | xargs chmod +x

      - name: Restore
        run: dotnet restore

      - name: Run tab perf smoke
        env:
          WRITE_METRICS_ARTIFACTS: "1"
          METRICS_ARTIFACT_DIR: ${{ github.workspace }}/artifacts/metrics
        run: dotnet test -c ${{ env.CONFIGURATION }} --no-build --no-restore --filter "Category=Performance|Category=Latency" --blame-hang-timeout 8m --blame-hang-dump-type none --logger "trx;LogFileName=tab_perf_smoke.trx"

      - name: Upload tab perf smoke artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: tab-perf-smoke
          retention-days: 5
          path: |
            **/*tab_perf_smoke.trx
            artifacts/metrics/**

  nightly_tab_stress:
    name: Nightly Tab Stress
    runs-on: ubuntu-latest
    timeout-minutes: 30
    if: github.event_name == 'schedule'
    needs: [build]

    steps:
      - uses: actions/checkout@v4

      - name: Cache .NET SDK
        uses: actions/cache@v4
        with:
          path: ~/.dotnet
          key: dotnet-sdk-${{ runner.os }}-${{ hashFiles('global.json') }}
          restore-keys: dotnet-sdk-${{ runner.os }}-

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        env:
          DOTNET_INSTALL_DIR: ~/.dotnet
        with:
          global-json-file: global.json
          cache: true
          cache-dependency-path: "**/*.csproj"

      - name: Download build artifact
        uses: actions/download-artifact@v4
        with:
          name: dotnet-build-ubuntu-latest
          path: tests

      - name: Restore execute permissions
        run: |
          find tests -path "*/bin/Release/net*" -type f \
            ! -name "*.dll" ! -name "*.json" ! -name "*.pdb" ! -name "*.xml" \
            ! -name "*.so" ! -name "*.dylib" ! -name "*.a" ! -name "*.txt" \
            | xargs chmod +x

      - name: Restore
        run: dotnet restore

      - name: Run stress lanes
        env:
          WRITE_METRICS_ARTIFACTS: "1"
          METRICS_ARTIFACT_DIR: ${{ github.workspace }}/artifacts/metrics
        run: dotnet test -c ${{ env.CONFIGURATION }} --no-build --no-restore --filter "Category=Stress|Category=Performance|Category=Latency|Category=RenderMetrics" --blame-hang-timeout 8m --blame-hang-dump-type none --logger "trx;LogFileName=nightly_tab_stress.trx"

      - name: Upload nightly stress artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: nightly-tab-stress
          retention-days: 7
          path: |
            **/*nightly_tab_stress.trx
            artifacts/metrics/**

  aot_publish:
    name: AOT Publish (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    if: github.event_name == 'workflow_dispatch'
    # 90 rather than 40: the signed mac lane waits synchronously on Apple's
    # notarization queue inside vpk pack, which alone can run 10-30+ minutes on a
    # slow day — 40 was cutting the job down mid-wait.
    timeout-minutes: 90
    needs: [build]
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            rid: win-x64
          - os: ubuntu-latest
            rid: linux-x64
          - os: macos-latest
            rid: osx-arm64

    # The secrets context is not allowed in step-level `if` (the workflow fails to
    # parse), so the signing gate is materialized here as an env boolean the macOS
    # keychain step can test. Mirrors release.yml's publish_aot.
    env:
      MAC_SIGNING_ENABLED: ${{ secrets.MAC_CERT_P12_BASE64 != '' }}

    steps:
      - uses: actions/checkout@v4

      - name: Cache .NET SDK
        uses: actions/cache@v4
        with:
          path: ~/.dotnet
          key: dotnet-sdk-${{ runner.os }}-${{ hashFiles('global.json') }}
          restore-keys: dotnet-sdk-${{ runner.os }}-

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        env:
          DOTNET_INSTALL_DIR: ~/.dotnet
        with:
          global-json-file: global.json
          cache: true
          cache-dependency-path: "**/*.csproj"

      - name: Download build artifact
        uses: actions/download-artifact@v4
        with:
          name: dotnet-build-${{ matrix.os }}
          path: tests

      - name: Restore
        run: dotnet restore

      # -p:SkipCliShim=true matches release.yml's Publish AOT step, and is required rather than
      # tidy. BuildCliShim invokes NovaTerminal.Cli out of band with -p:BuildProjectReferences=false
      # (that is what breaks the cycle - this project's build is what places the CLI beside it), and
      # the nested invocation carries no RuntimeIdentifier, so it looks for this project's reference
      # assembly at the RID-less obj/Release/net10.0/ref/. A self-contained RID publish writes its
      # intermediates under a win-x64/linux-x64/osx-arm64 subdirectory, so on a clean checkout that
      # path never exists and the nested build dies on a bare CS0006 naming it. Passing the RID down
      # does not help: the CLI references this project as an executable and then wants its apphost,
      # which a self-contained build never writes to obj. The shim is a framework-dependent artifact
      # and would be the wrong thing to embed here anyway - the AOT binary dispatches the CLI modes
      # itself (see Program.cs), which is why released bundles skip it too.
      - name: Publish AOT
        run: dotnet publish src/NovaTerminal.App/NovaTerminal.App.csproj -c ${{ env.CONFIGURATION }} -r ${{ matrix.rid }} --self-contained true -p:PublishAot=true -p:SkipCliShim=true -o artifacts/publish/${{ matrix.rid }}

      # ---- macOS packaging dry run (osx-arm64 only): the same vpk pack invocation
      # ---- release.yml runs, minus the download/upload-release steps. None of this is
      # ---- runnable from a Windows or Linux dev box, so this dispatch lane is the only
      # ---- way to verify the NovaTerminal.app bundle, the .pkg installer and the osx
      # ---- feed asset names BEFORE a real tag ships them. The vpk version is pinned to
      # ---- the same 1.2.0 as release.yml and Directory.Packages.props — the three must
      # ---- stay in lockstep.
      - name: Install vpk
        if: matrix.rid == 'osx-arm64'
        run: dotnet tool install -g vpk --version 1.2.0

      - name: Generate macOS app icon (.icns)
        if: matrix.rid == 'osx-arm64'
        run: packaging/macos/make-icns.sh src/NovaTerminal.App/Assets/nova_icon.png artifacts/icon/nova_icon.icns

      # Same secret-gated signing setup as release.yml's osx-arm64 lane (same action,
      # same gate), so this dispatch lane is also the pre-tag validation of the whole
      # codesign/notarize/staple path — not just of the unsigned packaging. When
      # MAC_CERT_P12_BASE64 is unset the step (and the flags below) is skipped and the
      # dry run packs unsigned, as it always did. See packaging/macos/README.md.
      - name: Set up macOS signing keychain
        id: mac_signing
        if: matrix.rid == 'osx-arm64' && env.MAC_SIGNING_ENABLED == 'true'
        uses: ./.github/actions/setup-mac-signing
        with:
          cert-p12-base64: ${{ secrets.MAC_CERT_P12_BASE64 }}
          installer-p12-base64: ${{ secrets.MAC_INSTALLER_CERT_P12_BASE64 }}
          cert-password: ${{ secrets.MAC_CERT_PASSWORD }}
          sign-app-identity: ${{ secrets.MAC_SIGN_APP_IDENTITY }}
          sign-install-identity: ${{ secrets.MAC_SIGN_INSTALL_IDENTITY }}
          notary-profile: ${{ secrets.MAC_NOTARY_PROFILE }}
          apple-id: ${{ secrets.MAC_APPLE_ID }}
          team-id: ${{ secrets.MAC_TEAM_ID }}
          app-specific-password: ${{ secrets.MAC_APP_SPECIFIC_PASSWORD }}

      # 0.0.1-ci: a dry-run version that can never collide with a real release's feed
      # (vpk rejects anything below 0.0.1, so 0.0.0-ci is not an option here).
      # Assertions mirror what release.yml asserts post-pack, so a silent vpk change
      # fails HERE rather than at the next tag.
      - name: Pack macOS bundle (Velopack dry run)
        if: matrix.rid == 'osx-arm64'
        shell: bash
        env:
          # Signing reaches the script through env, never interpolated into its source;
          # when the keychain step was skipped these are empty and vpk stays unsigned.
          MAC_SIGNING: ${{ steps.mac_signing.outputs.imported }}
          MAC_SIGN_KEYCHAIN: ${{ steps.mac_signing.outputs.keychain }}
          MAC_SIGN_APP_IDENTITY: ${{ secrets.MAC_SIGN_APP_IDENTITY }}
          MAC_SIGN_INSTALL_IDENTITY: ${{ secrets.MAC_SIGN_INSTALL_IDENTITY }}
          MAC_NOTARY_PROFILE: ${{ secrets.MAC_NOTARY_PROFILE }}
        run: |
          set -euo pipefail
          # StripSymbols=true leaves a ~94 MB NovaTerminal.dSYM beside the binary (3x the
          # binary's own size). Delete it at the source AND exclude it in vpk: deletion
          # keeps empty dSYM directory skeletons out of the bundle entirely, --exclude
          # (vpk's built-in default covers only .pdb) is defense-in-depth. Nothing else
          # consumes the symbols - the raw publish-dir zip this dSYM used to ship in no
          # longer exists for macOS.
          rm -rf artifacts/publish/osx-arm64/NovaTerminal.dSYM
          # Same secret-gated signing flags as release.yml (see its comments there);
          # the +guard keeps an empty array valid under set -u on bash 3.2.
          sign_args=()
          if [[ "${MAC_SIGNING:-}" == "1" ]]; then
            sign_args+=(
              --signAppIdentity "$MAC_SIGN_APP_IDENTITY"
              --signInstallIdentity "$MAC_SIGN_INSTALL_IDENTITY"
              --notaryProfile "$MAC_NOTARY_PROFILE"
              --keychain "$MAC_SIGN_KEYCHAIN"
            )
          fi
          vpk pack \
            --packId NovaTerminalApp \
            --packVersion 0.0.1-ci \
            --packDir artifacts/publish/osx-arm64 \
            --mainExe NovaTerminal \
            --packTitle NovaTerminal \
            --packAuthors benyblack \
            --bundleId com.benyblack.NovaTerminal \
            --icon artifacts/icon/nova_icon.icns \
            --exclude 'NovaTerminal\.dSYM' \
            --outputDir artifacts/velopack-dryrun \
            ${sign_args[@]+"${sign_args[@]}"}

          echo "Velopack dry-run output:"
          ls -l artifacts/velopack-dryrun

          test -n "$(ls artifacts/velopack-dryrun/*-osx-Setup.pkg 2>/dev/null || true)" || { echo "vpk pack produced no -osx-Setup.pkg" >&2; exit 1; }
          test -n "$(ls artifacts/velopack-dryrun/*-osx-Portable.zip 2>/dev/null || true)" || { echo "vpk pack produced no -osx-Portable.zip" >&2; exit 1; }
          test -f artifacts/velopack-dryrun/NovaTerminalApp-0.0.1-ci-osx-full.nupkg || { echo "vpk pack produced no full nupkg" >&2; exit 1; }
          test -f artifacts/velopack-dryrun/releases.osx.json || { echo "vpk pack produced no releases.osx.json" >&2; exit 1; }
          # StripSymbols leaves the dSYM beside the binary; the rm + --exclude above keep
          # it out of the bundle users download. Pin that: no dSYM entry may appear in
          # the Portable zip at all, not even an empty directory skeleton.
          if unzip -l artifacts/velopack-dryrun/NovaTerminalApp-osx-Portable.zip | grep -q "dSYM"; then
            echo "NovaTerminal.dSYM leaked into the Portable bundle" >&2
            exit 1
          fi

      - name: Upload macOS packaging dry-run artifacts
        if: matrix.rid == 'osx-arm64'
        uses: actions/upload-artifact@v4
        with:
          name: macos-packaging-dryrun
          retention-days: 7
          path: artifacts/velopack-dryrun

      - name: Upload AOT bundle
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: aot-${{ matrix.rid }}
          retention-days: 7
          path: artifacts/publish/${{ matrix.rid }}

  # ===========================================================================
  # Linux packaging: dry run + smoke gate (#91 Linux leg). Two callers of the same
  # scripts release.yml uses, so packaging breaks land on a PR rather than on a tag.
  #
  # THREE jobs, not one, and the split is load-bearing - read this before merging
  # them back together.
  #
  # 1. `linux_packaging_detect` (runner, no container): the change-detection gate.
  #    Cheap, needs `git` + full history, and its output gates the other two.
  # 2. `linux_packaging_build` (runner + `container: ubuntu:22.04`): everything that
  #    produces artifacts - cargo, AOT publish, build-deb.sh, vpk pack, assertions.
  # 3. `linux_packaging_smoke` (runner, no container): the docker-based gates -
  #    test-build-deb.sh and smoke-test.sh - consuming job 2's artifacts.
  #
  # WHY A CONTAINER AT ALL. NativeAOT links against the build machine's glibc with
  # no runtime fallback, so whatever builds the binary IS the minimum supported
  # distro. This lane used `runs-on: ubuntu-22.04` to pin that floor at glibc 2.35
  # (Ubuntu 22.04+, Debian 12+, Fedora 36+). But actions/runner-images#14254
  # deprecates the ubuntu-22.04 image starting 2026-09-17 (fully unsupported
  # 2027-04-17), which would have retired our glibc floor on GitHub's schedule
  # rather than ours. `runs-on: ubuntu-latest` + `container: ubuntu:22.04` pins the
  # floor to an image tag we control, permanently and independently of the runner
  # lifecycle. Bumping the floor is now a deliberate one-line edit here.
  #
  # WHY THE SMOKE GATE CANNOT BE IN THE CONTAINER. smoke-test.sh and
  # test-build-deb.sh both `docker run` bare containers themselves - that is the
  # entire design premise of the smoke test (a pristine machine that installs
  # nothing but the .deb). A `container:` job has no docker daemon and no socket, so
  # docker-in-docker is not available; mounting the host socket would be a
  # privilege escalation and would also break the `-v "$PWD:..."` mounts, because
  # paths inside the job container do not exist on the daemon's host. So the smoke
  # gate stays on the runner, where a daemon exists, and receives the artifacts via
  # upload/download-artifact. The gate is NOT dropped or weakened - it runs exactly
  # the same two scripts against exactly the artifacts job 2 produced.
  #
  # Artifact permissions are not preserved by upload/download-artifact. That is
  # harmless here: the .deb carries its own internal modes in its data tarball, and
  # smoke-test.sh already does `cp "$img" /tmp/nova.AppImage && chmod +x` before
  # launching an AppImage.
  #
  # x64 only: this lane exists to catch asset-naming and layout mistakes, which are
  # architecture-independent. release.yml covers arm64.
  # ===========================================================================
  #
  # This lane is deliberately NOT gated by a workflow-level `paths:` filter - that
  # would gate every other job in this file, and the `pull_request` trigger above
  # only has `paths-ignore`. Instead this job's `if:` stays event-based (always
  # eligible to run) and publishes a `run` output that gates the two expensive jobs:
  # workflow_dispatch is always a full dry run, push (to main, per the trigger above
  # - i.e. a post-merge run, not every commit) always runs fully too as a cheap
  # post-merge canary, and pull_request only runs fully when the diff against the
  # PR's merge base touches packaging/linux/** or a workflow file - otherwise the
  # ~40-minute AOT/build/smoke work never starts.
  #
  # This job replaces the per-step `if: steps.detect.outputs.run == 'true'` guards
  # the single-job version used. A job output is the only way to share the decision
  # across jobs, and it gives a truer run summary too: the expensive jobs now show
  # as "skipped" rather than as a green job of skipped steps. A failure INSIDE this
  # job still reds the workflow, same as the old failing detect step did.
  linux_packaging_detect:
    name: Linux Packaging (detect)
    runs-on: ubuntu-latest
    timeout-minutes: 10
    if: >-
      github.event_name == 'workflow_dispatch' ||
      github.event_name == 'push' ||
      github.event_name == 'pull_request'
    outputs:
      run: ${{ steps.detect.outputs.run }}
    steps:
      - uses: actions/checkout@v4
        with:
          # Full history, not the default shallow (depth-1) checkout: the change-
          # detection step below needs `git diff` against the PR's merge base, and
          # for a pull_request event the default checkout is the merge commit with
          # no parents fetched. A shallow checkout would make that diff wrong or
          # empty - silently disabling the PR gate this job exists to provide.
          fetch-depth: 0

      # Dependency-free path filter (no third-party action - this repo uses none
      # for path filtering elsewhere). workflow_dispatch and push always run the
      # full job; pull_request only runs it when the diff against the PR's merge
      # base touches packaging/linux/**, a workflow file, or one of the two other
      # inputs build-deb.sh reads directly (LICENSE, the app icon) - a change to
      # either would otherwise land invisibly, since neither lives under
      # packaging/linux/.
      - name: Detect packaging changes
        id: detect
        # Both values reach the script through env, never through `${{ }}` inside the
        # `run` block. Neither is attacker-controlled (github.event_name is a fixed
        # vocabulary, and base.sha is a 40-char hex the forge computes), so this is not
        # closing a live injection - it is the env-only rule release.yml already
        # documents and enforces for `run` inputs, applied here so the newest job is
        # not the one exception someone later cites as precedent.
        env:
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
        run: |
          set -euo pipefail
          if [[ "$EVENT_NAME" != "pull_request" ]]; then
            echo "run=true" >> "$GITHUB_OUTPUT"
            echo "$EVENT_NAME: always runs the full job."
            exit 0
          fi
          base="$BASE_SHA"
          echo "Diffing against PR merge base: $base"
          changed="$(git diff --name-only "$base"...HEAD)"
          echo "Changed files vs merge base:"
          echo "$changed"
          # <<< the here-string, not `echo ... | grep`: under pipefail, a changed-file
          # list exceeding the pipe buffer would make `echo` take SIGPIPE, the
          # pipeline non-zero, this `if` false, and the job skip itself SILENTLY -
          # the exact failure class this gate exists to prevent. smoke-test.sh and
          # build-deb.sh both avoid the same construct for the same reason.
          if grep -qE '^(packaging/linux/|\.github/workflows/[^/]+\.ya?ml$|LICENSE$|src/NovaTerminal\.App/Assets/nova_icon\.png$)' <<<"$changed"; then
            echo "run=true" >> "$GITHUB_OUTPUT"
            echo "Packaging or workflow files changed - running the full job."
          else
            echo "run=false" >> "$GITHUB_OUTPUT"
            echo "No packaging/linux or workflow changes detected - skipping the packaging build."
          fi

  linux_packaging_build:
    name: Linux Packaging (dry run)
    needs: linux_packaging_detect
    if: needs.linux_packaging_detect.outputs.run == 'true'
    runs-on: ubuntu-latest
    # The glibc floor. See the block comment above - this tag, not the runner
    # image, is what defines the minimum distro NovaTerminal's Linux build
    # supports. Do not bump it without deciding to drop a distro.
    container:
      image: ubuntu:22.04
    # THE FIX for the first real CI failure of this lane: inside a `container:`
    # job GitHub does NOT default the shell to bash the way it does on a plain
    # runner - it invokes the inline `run:` blocks under `sh`, which on this
    # image is dash. Every step below opens with `set -euo pipefail`, and dash
    # has no `pipefail` option at all ("set: Illegal option -o pipefail"), so
    # the very first step failed on line 1 before anything else ran. Every local
    # verification of this recipe used `docker run ... bash -c '...'` explicitly,
    # so this gap was invisible until GitHub picked the shell itself. Declaring
    # the job-level default is the systemic fix - it also covers the `<<<`
    # here-strings and other bash-only constructs used later in this job's
    # `run:` blocks, none of which dash supports either.
    defaults:
      run:
        shell: bash
    # 40 -> 60: uncached cargo (two crates) + full NuGet restore + AOT publish +
    # vpk pack, cold, plus - new with the container - an SDK download and an
    # apt-install of the entire toolchain on every run that misses the caches.
    # aot_publish needed the same kind of bump (40 -> 90) for a lighter workload.
    # This is a capacity estimate for a cold job, not a flake being papered over -
    # if it ever flakes AFTER caching, that is a hypothesis to investigate, not a
    # number to bump again.
    timeout-minutes: 60
    steps:
      # MUST be the first step, before actions/checkout: a bare ubuntu:22.04 has
      # no git, and actions/checkout silently falls back to a tarball download via
      # the REST API when git is absent - which produces no `.git` at all, so
      # `fetch-depth` and anything git-shaped downstream would be meaningless.
      #
      # Everything the pipeline needs is installed explicitly. A bare image has
      # almost nothing, and this job's standing philosophy ("relying on image
      # contents is what breaks when the image rolls") is now not a philosophy but
      # a hard requirement:
      #   ca-certificates curl  - dotnet-install.sh, rustup, TLS for both
      #   git                   - actions/checkout (see above)
      #   unzip                 - the nupkg .dbg leak assertion below
      #   zstd, tar, gzip       - actions/cache's archive backends (tar/gzip are in
      #                           the base image; zstd is not, and cache silently
      #                           degrades to gzip without it)
      #   clang zlib1g-dev      - the NativeAOT link step (documented prerequisites)
      #   binutils              - `strip`, which build-deb.sh preflights and fails
      #                           loudly without
      #   file                  - build-deb.sh's ELF discovery for Depends: walking
      #   imagemagick           - `convert`, for the hicolor icon set
      #   squashfs-tools        - `mksquashfs`. vpk pack builds a type-2 AppImage
      #                           and CANNOT produce one without it; a bare
      #                           ubuntu:22.04 does not ship it, and the local
      #                           end-to-end only ever produced an AppImage once it
      #                           was installed. Not optional.
      #   fontconfig libfreetype6 x11-utils
      #                         - build-deb.sh's Depends: derivation walks ldd
      #                           against whatever is ALREADY resolvable on this
      #                           machine, so leaving these implicit makes the
      #                           derived set a function of imagemagick's own
      #                           dependency graph on whichever apt snapshot is
      #                           current - which is exactly how a 24.04 run and a
      #                           22.04 run produced two different transitive sets
      #                           from identical binaries. Declaring them makes it a
      #                           function of this list instead.
      #   libicu70 libssl3      - the .NET SDK's own runtime prerequisites on 22.04
      - name: Install container toolchain
        run: |
          set -euo pipefail
          export DEBIAN_FRONTEND=noninteractive
          apt-get update
          apt-get install -y \
            ca-certificates curl git unzip zstd \
            clang zlib1g-dev binutils file imagemagick squashfs-tools \
            fontconfig libfreetype6 x11-utils \
            libicu70 libssl3
          echo "--- container identity (the glibc floor this lane pins) ---"
          cat /etc/os-release | head -2
          ldd --version | head -1

      - uses: actions/checkout@v4

      # Cache the SDK payload itself, keyed on global.json: dotnet-install.sh is a
      # ~220 MB download on every cold run, and unlike the runner image the
      # container has no preinstalled SDK to fall back on. dotnet-install.sh
      # no-ops when the requested version is already present in --install-dir, so a
      # cache hit skips the download entirely.
      - name: Cache .NET SDK
        uses: actions/cache@v4
        with:
          path: ~/.dotnet
          key: dotnet-sdk-container-ubuntu2204-${{ hashFiles('global.json') }}
          restore-keys: dotnet-sdk-container-ubuntu2204-

      # actions/setup-dotnet is deliberately NOT used here. It is a node action
      # that shells out to this same dotnet-install.sh, so it would add a layer
      # whose behaviour inside a bare container is unproven, to gain only the
      # `cache: true` NuGet cache - which is one actions/cache block below.
      # Calling the script directly is what was actually verified end-to-end in
      # ubuntu:22.04.
      #
      # --jsonfile global.json, NOT --version <literal>: the SDK version is read
      # out of global.json itself, so this can never drift from the pin. Verified:
      # `dotnet-install.sh --jsonfile global.json --dry-run` resolves to
      # dotnet-sdk-10.0.400-linux-x64, matching global.json's `sdk.version`.
      - name: Install .NET SDK (version pinned by global.json)
        run: |
          set -euo pipefail
          curl -sSL --proto '=https' --tlsv1.2 https://dot.net/v1/dotnet-install.sh -o /tmp/dotnet-install.sh
          chmod +x /tmp/dotnet-install.sh
          /tmp/dotnet-install.sh --jsonfile global.json --install-dir "$HOME/.dotnet"
          # Both entries: `dotnet` itself, and the global-tool shim dir that
          # `dotnet tool install -g vpk` writes into. Neither is on PATH in a
          # container, unlike on the hosted runner image.
          echo "$HOME/.dotnet" >> "$GITHUB_PATH"
          echo "$HOME/.dotnet/tools" >> "$GITHUB_PATH"
          echo "DOTNET_ROOT=$HOME/.dotnet" >> "$GITHUB_ENV"

      - name: Verify dotnet is on PATH
        run: |
          set -euo pipefail
          command -v dotnet
          dotnet --version

      # setup-dotnet's `cache: true` (used by the other jobs in this file) is not
      # available without setup-dotnet, so the NuGet package cache is declared
      # directly with the same dependency path it would have used.
      - name: Cache NuGet packages
        uses: actions/cache@v4
        with:
          path: ~/.nuget/packages
          key: nuget-container-ubuntu2204-${{ hashFiles('**/*.csproj') }}
          restore-keys: nuget-container-ubuntu2204-

      # dtolnay/rust-toolchain@stable is not used here: it drives an EXISTING
      # rustup, which the hosted runner image preinstalls and a bare
      # ubuntu:22.04 does not. Same intent, via the upstream installer.
      #
      # The upstream installer is NOT invoked as `curl https://sh.rustup.rs | sh`,
      # though: this pipeline produces artifacts end users install, and a release
      # pipeline should not execute an unverified script fetched through a
      # redirector. Instead: download a version-pinned rustup-init for this
      # container's own architecture, verify it against rustup's published
      # SHA256 before executing anything, then run the verified binary.
      # RUSTUP_VERSION is pinned in a variable (not buried in a URL) so the pin
      # is visible and trivial to bump. `uname -m` maps directly onto the
      # "<arch>-unknown-linux-gnu" triple name for both x86_64 and aarch64, so
      # no per-arch branch is needed - this runs unchanged on the
      # ubuntu-24.04-arm leg.
      - name: Install Rust
        run: |
          set -euo pipefail
          RUSTUP_VERSION=1.29.1
          triple="$(uname -m)-unknown-linux-gnu"
          base_url="https://static.rust-lang.org/rustup/archive/${RUSTUP_VERSION}/${triple}"
          curl --proto '=https' --tlsv1.2 -sSf -o /tmp/rustup-init "${base_url}/rustup-init"
          curl --proto '=https' --tlsv1.2 -sSf -o /tmp/rustup-init.sha256 "${base_url}/rustup-init.sha256"
          (cd /tmp && sha256sum -c rustup-init.sha256)
          chmod +x /tmp/rustup-init
          /tmp/rustup-init -y --default-toolchain stable --profile minimal
          echo "$HOME/.cargo/bin" >> "$GITHUB_PATH"

      - name: Verify cargo is on PATH
        run: |
          set -euo pipefail
          command -v cargo
          cargo --version
          rustc --version

      # Kept as-is: a node action, so it runs in the container, and it only needs
      # cargo on PATH - which the step above put there via $GITHUB_PATH.
      - name: Rust incremental build cache
        uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2
        with:
          workspaces: src/NovaTerminal.App/native

      - name: Build Rust native
        run: |
          cd src/NovaTerminal.App/native
          cargo build --release --locked
          mkdir -p target_linux/release
          cp target/release/librusty_pty.so target_linux/release/
          cd rusty_ssh && cargo build --release --locked

      - name: Restore
        run: dotnet restore

      # -p:SkipCliShim=true for the same reason as release.yml's Publish AOT: the
      # shim needs a RID-less framework-dependent build, and the AOT binary
      # dispatches the CLI modes itself.
      - name: Publish AOT (linux-x64)
        env:
          SKIP_RUST_NATIVE_BUILD: "1"
        run: >-
          dotnet publish src/NovaTerminal.App/NovaTerminal.App.csproj
          -c ${{ env.CONFIGURATION }} -r linux-x64 --self-contained true
          -p:PublishAot=true -p:SkipCliShim=true
          -p:Version=0.0.1-ci -p:InformationalVersion=0.0.1-ci
          -o artifacts/publish/linux-x64

      - name: Build .deb
        run: |
          chmod +x packaging/linux/*.sh
          packaging/linux/build-deb.sh artifacts/publish/linux-x64 0.0.1-ci amd64 artifacts/linux

      # Recorded, not asserted: the derived Depends: is a function of what is
      # installed on the machine doing the packaging (see the toolchain step's
      # note on fontconfig), so printing it makes an image-driven change visible
      # in the log instead of only showing up as a smoke-test failure.
      - name: Show derived Depends
        run: dpkg-deb -f artifacts/linux/novaterminal_0.0.1~ci-1_amd64.deb Depends

      # vpk pinned to 1.2.0, in lockstep with Directory.Packages.props and the two
      # other install sites in these workflows.
      - name: Install vpk
        run: dotnet tool install -g vpk --version 1.2.0

      # --channel linux-x64, NOT the platform default 'linux'. Linux publishes two
      # architectures into one GitHub release, and a Velopack feed is per-channel,
      # not per-architecture, so a shared 'linux' channel would offer an arm64
      # client an x64 package. Must match VelopackUpdateService.ResolveExplicitChannel.
      #
      # 0.0.1-ci because vpk rejects anything below 0.0.1. No --delta concerns here:
      # there is no prior release in this scratch output directory, so a full-only
      # pack is expected and correct for a dry run.
      # StripSymbols=true (NovaTerminal.App.csproj) emits a ~79 MB NovaTerminal.dbg
      # beside the AOT binary on Linux - the same class of leak the macOS lane
      # already guards against for its dSYM (delete at the source AND --exclude in
      # the pack, see that job's comments). build-deb.sh already deletes *.dbg
      # before staging the .deb, so only this AppImage/nupkg path was exposed:
      # every full-nupkg download was roughly tripling, silently, and Task 5 was
      # about to copy this exact invocation into the real release lane.
      #
      # One --exclude, not two: `vpk pack --help` documents it as a single REGEX
      # value ("[default: .*\.pdb]"), so both patterns are combined into one
      # alternation rather than passed twice (unverified whether a second
      # occurrence would error or silently overwrite the first - not worth finding
      # out on the release-lane-adjacent invocation).
      #
      # mksquashfs (squashfs-tools, installed above) is a hard prerequisite of this
      # step - a type-2 AppImage IS a squashfs image.
      - name: Pack AppImage (Velopack)
        run: |
          rm -f artifacts/publish/linux-x64/*.pdb artifacts/publish/linux-x64/*.dbg
          vpk pack \
            --packId NovaTerminalApp \
            --packVersion 0.0.1-ci \
            --packDir artifacts/publish/linux-x64 \
            --mainExe NovaTerminal \
            --packTitle NovaTerminal \
            --packAuthors benyblack \
            --channel linux-x64 \
            --icon src/NovaTerminal.App/Assets/nova_icon.png \
            --exclude '.*\.pdb|NovaTerminal\.dbg' \
            --outputDir artifacts/linux

      # Mirrors release.yml's post-pack assertions, so a silent vpk behaviour change
      # surfaces here instead of on a tag. The `~` in the .deb filename is the
      # prerelease mapping from build-deb.sh - asserting it here pins that behaviour.
      - name: Assert expected assets
        run: |
          # set -euo pipefail is not the default here: GitHub runs `bash -e {0}`, with
          # neither `pipefail` nor `nounset`. Without pipefail a failing left-hand side
          # of the `unzip | grep` below was invisible - see the leak assertion's own
          # comment at the bottom of this step.
          set -euo pipefail
          cd artifacts/linux
          ls -la
          test -n "$(ls ./*.AppImage 2>/dev/null)" || { echo "vpk pack produced no .AppImage" >&2; exit 1; }
          test -f novaterminal_0.0.1~ci-1_amd64.deb || { echo "no .deb (or wrong prerelease version mapping)" >&2; exit 1; }
          test -f NovaTerminalApp-0.0.1-ci-linux-x64-full.nupkg || { echo "no linux-x64 full nupkg" >&2; exit 1; }
          test -f releases.linux-x64.json || { echo "no releases.linux-x64.json" >&2; exit 1; }
          dpkg-deb --contents novaterminal_0.0.1~ci-1_amd64.deb
          # StripSymbols leaves NovaTerminal.dbg beside the binary; the rm + --exclude
          # in the Pack step above keep it out of what users/updaters download. Pin
          # that here too: no .dbg entry may appear in the full nupkg at all - a
          # .nupkg is a plain zip, so this mirrors the macOS lane's dSYM leak
          # assertion against its Portable.zip exactly.
          #
          # THIS CHECK USED TO BE ABLE TO PASS WITHOUT CHECKING ANYTHING, and the shape
          # it used is the reason - do not revert to it. It was
          #   if unzip -l <nupkg> | grep -q '\.dbg'; then ... fi
          # and GitHub's default shell is `bash -e {0}` with NO pipefail. So with `unzip`
          # absent - one edit to the apt list above away - the pipeline's status is
          # `grep`'s, which is 1 on empty input; a non-zero status in an `if` CONDITION
          # does not trip `set -e`; the `if` simply went false and the step passed green
          # having inspected nothing. Three things now make that impossible:
          #   1. `set -euo pipefail` at the top of the step,
          #   2. an explicit `command -v unzip` preflight (pipefail alone is NOT enough -
          #      a non-zero pipeline inside an `if` condition is still just "false"),
          #   3. running unzip in a command substitution FIRST, where `set -e` does see
          #      its failure, and grepping the captured listing via a here-string (also
          #      avoiding the SIGPIPE-on-large-output hazard documented in
          #      linux_packaging_detect).
          command -v unzip >/dev/null \
            || { echo "unzip is not installed, so the .dbg leak assertion cannot run. Add it back to the container toolchain step - do not let this check silently no-op." >&2; exit 1; }
          nupkg_listing="$(unzip -l NovaTerminalApp-0.0.1-ci-linux-x64-full.nupkg)"
          if grep -q '\.dbg' <<<"$nupkg_listing"; then
            echo "NovaTerminal.dbg leaked into the linux-x64 full nupkg" >&2
            exit 1
          fi

      # if: always() so a failure in the assertions above still leaves the produced
      # artifacts downloadable for triage. This upload is also the ONLY channel by
      # which the smoke job below receives them, so its name and the download step's
      # name must stay in sync.
      - name: Upload dry-run artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: linux-packaging-dryrun
          retention-days: 5
          path: |
            artifacts/linux/*.AppImage
            artifacts/linux/*.deb
            artifacts/linux/*.nupkg
            artifacts/linux/releases.*.json

  # The docker half. Runs on the runner, NOT in a container, because both scripts
  # below start their own bare containers - see the block comment at the top of
  # this lane for why docker-in-docker is not an option. This is the release gate:
  # if it is ever skipped or made non-blocking, the lane has lost its point.
  linux_packaging_smoke:
    name: Linux Packaging (smoke)
    needs: [linux_packaging_detect, linux_packaging_build]
    if: >-
      needs.linux_packaging_detect.outputs.run == 'true' &&
      needs.linux_packaging_build.result == 'success'
    runs-on: ubuntu-latest
    # Two cold smoke containers that each apt-install their own tooling, plus the
    # test-build-deb.sh harness container. No compilation happens here.
    timeout-minutes: 30
    steps:
      # The scripts live in the repo, the artifacts come from the build job.
      - uses: actions/checkout@v4

      - name: Download dry-run artifacts
        uses: actions/download-artifact@v4
        with:
          name: linux-packaging-dryrun
          path: artifacts/linux

      - name: Show downloaded artifacts
        run: ls -la artifacts/linux

      # fontconfig + x11-utils: test-build-deb.sh's own header comment documents
      # these as the donor-ELF providers (fc-match, xdpyinfo) its dedupe check
      # needs to be non-vacuous. Without them here, fc-match/xdpyinfo are absent
      # too, and the harness's donor search falls back to whatever imagemagick
      # happened to pull in transitively - which is exactly the "only
      # libfontconfig1 is discriminating" gap this list closes. binutils: strip's
      # preflight in build-deb.sh fails loudly without it.
      #
      # ubuntu:22.04 here, matching the build job's container: this harness runs
      # build-deb.sh, so it must see the same distro the real .deb was built on.
      - name: Test build-deb.sh
        run: |
          chmod +x packaging/linux/*.sh
          docker run --rm -v "$PWD:/w:ro" -w /w ubuntu:22.04 bash -c \
            'apt-get update -qq && apt-get install -y -qq file imagemagick fontconfig x11-utils binutils >/dev/null &&
             packaging/linux/test-build-deb.sh'

      - name: Smoke test (bare containers)
        run: |
          chmod +x packaging/linux/*.sh
          packaging/linux/smoke-test.sh artifacts/linux
